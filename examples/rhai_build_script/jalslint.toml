# The JVM launcher fixes `main`'s signature, so `String[] args` is written whether or not the
# program reads it. This one prints what the build script generated and takes no arguments, so
# `unused-variables` would flag a parameter the example cannot drop — the suppression the rule's
# own documentation prescribes for a signature it does not get to choose.
[unused]
unused-variables = "allow"
