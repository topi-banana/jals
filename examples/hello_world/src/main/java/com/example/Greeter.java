package com.example;

/**
 * A second source file, so the example exercises multi-file compilation: jals
 * discovers every .java file under `source-dirs` and hands them all to javac.
 */
public class Greeter {
    public String greet(String name) {
        return "Hello, " + name + "!";
    }
}
